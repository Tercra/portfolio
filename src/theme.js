import { createTheme } from "@mui/material/styles";

const twilight = createTheme({
    dark : '#292831',
    lightDark : '#333f58',
    turquoise : '#4a7a96',
    peach : '#ee8695',
    lightPeach : '#fbbbad'
});

export default twilight;